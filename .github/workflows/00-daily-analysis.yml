          # OpenAI 渠道（LLM_CHANNELS=openai 时生效；Key 复用 legacy OPENAI_API_KEY）
          LLM_OPENAI_API_KEY: ${{ secrets.LLM_OPENAI_API_KEY || secrets.OPENAI_API_KEY }}
          LLM_OPENAI_API_KEYS: ${{ secrets.LLM_OPENAI_API_KEYS || secrets.OPENAI_API_KEYS }}
